{
  "name": "store-example",
  "version": "0.0.28",
  "private": true,
  "scripts": {
    "dev": "next dev -p $PORT",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "gh-pages": "npm run build; npm run export; rm -rf ../store-gh-pages/*; mv ./out/* ../store-gh-pages/; rm -rf out",
    "gitpod": "export $(grep -v '^#' .env | xargs); npm run dev"
  },
  "dependencies": {
    "@deepcase/store": "^0.0.32",
    "next": "10.0.7",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/react": "^17.0.2"
  }
}
